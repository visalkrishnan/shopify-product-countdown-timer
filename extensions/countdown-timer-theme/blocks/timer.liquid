<div 
  class="product-countdown-timer-app"
  data-product-id="{{ product.id }}"
  data-shop="{{ shop.permanent_domain }}"
>
  <!-- React/Preact will mount here -->
  <div class="timer-loading">Loading offer...</div>
</div>

<script src="{{ 'timer.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Countdown Timer",
  "target": "section",
  "settings": []
}
{% endschema %}